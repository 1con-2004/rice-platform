# Docker开发环境配置
# Database Configuration
DB_HOST=db
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root
DB_NAME=rice_platform

# JWT Configuration
JWT_SECRET=rice_platform_secret_key_2024_docker
JWT_EXPIRES_IN=24h

# Server Configuration
PORT=4001
NODE_ENV=docker